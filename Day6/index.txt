#github Assignment

Q1:
ssh-keygen -t ed25519 -C "mj370207@gmail.com"
cat ~/.ssh/id_ed25519.pub

Q2:
creating in github respos and respos name is githubPractice

Q3:
git clone https://github.com/manojjaiswal15/githubPractice.git

Q4:
git add test.txt
git commit -m "test.txt add"
git push origin -u main

Q5:
git branch "f1"
git branch "f2"


Q6:
git checkout f1
abc.txt   adding text "Change made in branch f1"
git add .
git commit -m "f1"

git checkout f2
abc.txt   adding text "Different change made in branch f2"
git add .
git commit -m "f2"

git merge f1     now its merge occur then im check what is add in and ignore then commit


Q7:
when two branches have developed different commit histories after their common ancestor

Q8:
git init :- its used for initialized the respos and this make from scratch.
git clone:- Creates a copy of an existing Git repository

Q9:
git branch:- its used for create,delete,rename for branch and they dont switch for used
git checkout:- Switches to another branch or commit and creating branch

Q10:
git fetch :- Downloads new data (commits, branches, tags) from remote and they dont change the code in local machine
git pull :- Fetches and then merges changes from remote into your current branch

Q11:


Q12:
git reset :- Moves the branch pointer to an earlier commit
git revert :- Creates a new commit that undoes changes from a previous commit

Q13:
git branch -d f2